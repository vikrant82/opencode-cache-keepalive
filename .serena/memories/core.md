# Core

## Project knowledge
- Cache-warming architecture, server/TUI IPC, and behavioral invariants: `mem:architecture/keepalive`.
- Toolchain, package surface, and commands: `mem:tech_stack`, `mem:suggested_commands`, `mem:task_completion`.
- Formatting and TypeScript conventions: `mem:conventions`.